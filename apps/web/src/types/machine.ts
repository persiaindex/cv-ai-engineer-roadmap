export type MachineApiItem = {
  id: number
  machine_id: string
  name: string
  location: string
  is_active: boolean
  created_at: string
}

export type Machine = {
  id: number
  machineId: string
  name: string
  location: string
  isActive: boolean
  createdAt: string
}